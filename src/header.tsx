import { useState } from 'preact/hooks';
import './style.css';

export const Header = () => {

    return (
        <div class="header">
            <div class="header-logo">Path of Lava</div>
        </div>
    );
}